import { Component, Disposable } from "../../base-types";
import { FragmentClassifier, FragmentManager } from "../index";
export declare class FragmentExploder extends Component<Set<string>> implements Disposable {
    fragments: FragmentManager;
    groups: FragmentClassifier;
    name: string;
    height: number;
    groupName: string;
    enabled: boolean;
    private _explodedFragments;
    get(): Set<string>;
    constructor(fragments: FragmentManager, groups: FragmentClassifier);
    dispose(): void;
    explode(): void;
    reset(): void;
    update(): void;
}
